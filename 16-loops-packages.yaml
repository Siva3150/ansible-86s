- name: loops 
  hosts: local
  connection: local 
  become: yes
  tasks:
  - name: Installing packages 
    ansible.builtin.package:
     name: "{{ item }}"
     state: latest  
    loop:
    - git
    - nginx 
    - mysql 
    - zip